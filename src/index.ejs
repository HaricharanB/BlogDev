<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">

</head>
<body>

<h1>All Posts</h1>
<a href="/add">Add New Blog</a>
<a href="/logout">Logout</a>
<ul>
  <% blogs.forEach(blog => { %>
    <li>
      <h3><%= blog.title %></h3>
      <p><%= blog.content %></p>
      <p><small>By <%= blog.username %></small></p>
      <p><small>User Id is : <%= blog.user_id %></small></p>
      <% if (blog.user_id === session.userId) { %>
        <a href="/edit/<%= blog.id %>">Edit</a>
        <form action="/delete/<%= blog.id %>" method="POST">
          <button type="submit" style="width: 100%; height: 100%;">Delete</button>
        </form>
      <% } %>
    </li>
  <% }) %>
</ul>
    
</body>
</html>

